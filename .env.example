# Absolute backend URL used by the app (and the dev proxy).
# Leave unset to call the relative /api path (dev proxy or same-origin reverse proxy).
# Example: VITE_API_URL=http://localhost:4000
# Example: VITE_API_URL=https://api.example.com
VITE_API_URL=
